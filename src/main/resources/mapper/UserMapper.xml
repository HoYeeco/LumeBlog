<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hoico.dao.UserDAO">

    <select id="findByUsername" parameterType="String" resultType="com.hoico.model.User"> SELECT *
        FROM user WHERE username = #{username} </select>

    <select id="findById" parameterType="Integer" resultType="com.hoico.model.User"> SELECT * FROM
        user WHERE id = #{id} </select>

    <select id="findAll" resultType="com.hoico.model.User"> SELECT * FROM user </select>

    <insert id="insert" parameterType="com.hoico.model.User"> INSERT INTO user (username, password,
        email, nickname, role, create_time, update_time) VALUES (#{username}, #{password}, #{email}, #{nickname},
        #{role}, #{createTime}, #{updateTime}) </insert>

    <update id="update" parameterType="com.hoico.model.User"> UPDATE user SET username =
        #{username}, password = #{password}, email = #{email}, nickname = #{nickname}, role = #{role}, update_time =
        #{updateTime} WHERE id = #{id} </update>
</mapper>
